# 다음 단계 가이드

## 완료된 작업

### Phase 1: 백엔드 개발 (2026-02-08)

- [x] 환경 설치 (pip install, playwright install chromium)
- [x] API 디스커버리 실행 → 207개 엔드포인트 발견
- [x] 엔드포인트 업데이트 (`kb_endpoints.py` 실제 경로로 교체)
- [x] 파서 조정 (KB시세 실제 응답 구조 반영)
- [x] 모듈 임포트 검증 (16/16 OK)
- [x] FastAPI 앱 로딩 검증 (26 라우트)
- [x] 버그 수정 (`price_data.py` Boolean 임포트 누락)

### Phase 2: 프론트엔드 개발 (2026-02-09)

- [x] React 19 + Vite 7 프로젝트 생성
- [x] Tailwind CSS v4 + shadcn/ui 설정
- [x] TypeScript 타입 정의 (complex, job, run, data)
- [x] API 클라이언트 (axios) + TanStack Query 훅
- [x] 레이아웃 (AppShell, Sidebar, PageHeader)
- [x] 대시보드 페이지 (통계 카드 + 최근 실행 + 차트)
- [x] 단지 관리 (목록/상세/등록/지역발견)
- [x] 수집 작업 (목록/생성/실행/일시정지/재개/지역수집)
- [x] 실행 이력 (목록/상세/태스크 필터)
- [x] 데이터 탐색 (시세 차트+테이블/실거래가/매물 + CSV 내보내기)
- [x] TypeScript + Vite 빌드 통과

### Phase 3: 인프라 구축 (2026-02-09)

- [x] Docker Compose로 PostgreSQL + Redis 시작
- [x] 포트 충돌 해결 (로컬 PG 5432 → Docker 5433)
- [x] `alembic.ini` 버그 수정 (`script_location`)
- [x] Alembic 마이그레이션 생성 + 적용 (10개 테이블)
- [x] FastAPI 백엔드 기동 확인 (26 routes, Swagger OK)
- [x] Celery Worker 기동 확인 (8 tasks registered)
- [x] Frontend dev server 기동 확인 (Vite, port 5174)
- [x] 전체 시스템 통합 검증 (API 응답 200 OK)

> 상세 검증 결과: [05-개발-검증-결과.md](05-개발-검증-결과.md) 참조
> 프론트엔드 상세: [06-프론트엔드-및-기동-가이드.md](06-프론트엔드-및-기동-가이드.md) 참조

---

## 즉시 해야 할 것

### 1단계: 단지 발견 + 데이터 수집 E2E 테스트

모든 서비스가 기동된 상태에서 실제 크롤링을 테스트합니다.

**웹 UI 방식:**
1. http://localhost:5173 접속
2. 단지 관리 → "지역 발견" → 강남구 `11680` 선택
3. 수집 작업 → "지역 수집" → `11680` 입력 → 수집 시작
4. 실행 이력에서 진행 상태 확인
5. 데이터 탐색에서 수집된 데이터 확인

**CLI 방식:**
```bash
# 강남구(11680) 아파트 단지 자동 발견
curl -X POST "http://localhost:8000/api/complexes/discover-region?region_code=11680"

# 지역 전체 수집 (발견→시세→실거래가→매물)
curl -X POST "http://localhost:8000/api/jobs/run-region?region_code=11680"

# 수집 결과 확인
curl "http://localhost:8000/api/runs/?limit=5"
curl "http://localhost:8000/api/data/kb-prices?complex_id=1"
```

### 2단계: 매물 API 파라미터 확인

매물 API(`propList/stutCdFilter`)의 정확한 파라미터 조합 확인 필요.

```bash
# Playwright 네트워크 인터셉트로 매물 탭 클릭 시 전송되는 요청 관찰
python -m scripts.run_api_discovery \
  --complex-url "https://kbland.kr/map?complexNo=23511" \
  -o listing_discovery.json -v
```

### 3단계: 실거래가 데이터 검증

실거래가 데이터가 있는 단지를 찾아 파서 검증 필요.
(테스트 단지 ID=12에는 실거래가 데이터 없음)

---

## KB API 직접 테스트 (DB 없이)

```bash
# 시세 API 직접 호출
python -c "
import httpx, json
r = httpx.get('https://api.kbland.kr/land-price/price/BasePrcInfoNew',
    params={'단지기본일련번호': '12', '면적일련번호': '127753'},
    headers={'Referer': 'https://kbland.kr', 'Origin': 'https://kbland.kr'})
data = r.json()
sise = data['dataBody']['data']['시세'][0]
print(f'일반가: {sise[\"매매일반거래가\"]:,}만원')
print(f'상한가: {sise[\"매매상한가\"]:,}만원')
print(f'하한가: {sise[\"매매하한가\"]:,}만원')
print(f'기준일: {sise[\"시세기준년월일\"]}')
"

# 단지 검색 API 직접 호출
python -c "
import httpx, json
r = httpx.post('https://api.kbland.kr/land-complex/map/map250mBlwInfoList',
    json={'물건종류': '01', 'startLat': 37.48, 'startLng': 127.01,
          'endLat': 37.54, 'endLng': 127.07, '거래유형': '1,2,3',
          'selectCode': '1,2,3', 'zoomLevel': 14},
    headers={'Referer': 'https://kbland.kr', 'Origin': 'https://kbland.kr'})
data = r.json()['dataBody']['data']
complexes = data.get('단지리스트', data.get('list', []))
print(f'발견된 단지: {len(complexes)}개')
for c in complexes[:5]:
    print(f'  {c.get(\"단지명\", \"?\")} (ID: {c.get(\"단지기본일련번호\", \"?\")})')
"
```

---

## 5월 추가 완료 항목

- [x] 매물 API 2단계 (brif → propList/main) 검증 및 적용
- [x] 실거래 — `preSalePrices` 엔드포인트로 전환 (vlaDealDtailPriceInq No Data 회피)
- [x] 시설 수집 — KBFacility (학군/지하철/병원) + OSM Overpass (공원)
- [x] 단지 dong_code / lat / lng 보정 (`ensure_complex_detail`)
- [x] 시설 그룹 모델 `complex_facilities` + 학군 거리 cutoff
- [x] **RedBeat** 도입 — 사용자 cron 변경 즉시 반영
- [x] 시도/시군구/동 단위 cron 배치 (`/api/batches/scoped/*`)
- [x] **Asia/Seoul** timezone 통일 (`enable_utc=False`, `core/time.now_kst`)
- [x] **transient retry 통합** — `kb_base._fetch_via_http` (RemoteProtocolError 등)
- [x] **prefork + fork-safety env** (macOS SIGSEGV 회피)
- [x] **listings.ask_price BigInteger** 마이그레이션 (21억+ overflow 해결)
- [x] **UNIQUE 인덱스** 정비 + 중복 정리 (`idx_transaction_unique`, `idx_kb_price_unique`, `ix_listings_source_listing_id`)
- [x] **UPSERT 전환** — Transaction / ComplexFacility ON CONFLICT DO NOTHING
- [x] **좀비 cleanup task** (5분 주기, task 10분 / run 60분 cutoff)
- [x] 단지 상세 — 시세/실거래/매물 탭 통합 (데이터 탐색 메뉴 흡수)

> 5월 종합 현황: [09-2026-05-현황-요약.md](09-2026-05-현황-요약.md)
> 5월 안정성 보강 상세: [08-크롤링-안정성-보강-2026-05.md](08-크롤링-안정성-보강-2026-05.md)

## 보류 / 다음 단계

| 우선순위 | 작업 | 설명 |
|----------|------|------|
| 중간 | DatabaseTask thread-safe 화 | `_db` 가 클래스 변수라 threads pool 사용 시 race. 현재 prefork 로 우회 |
| 중간 | 실패 task 자동 재시도 cron | 현재는 좀비 cleanup 만, 일시적 실패 자동 retry 미구현 |
| 중간 | API 응답 구조 변경 감지 | 스키마 검증 (깨진 파서 조기 발견) |
| 중간 | Scoped batch (dong_code) 운영 검증 | 도입 직후, 장기 운영 데이터 부족 |
| 낮음 | 모니터링 대시보드 | 실패율/좀비 카운트/throughput 시각화 |
| 낮음 | 수집 실패율 알림 | Slack/이메일 |
| 낮음 | Docker 통합 빌드 | worker.Dockerfile + Playwright 시스템 의존성 |
| 낮음 | 프론트엔드 프로덕션 빌드 | Vite build + nginx 또는 FastAPI 정적 서빙 |
| 낮음 | floor NULL UNIQUE 처리 | NULL → 0 정규화 (현재 무시) |

---

## 파일 변경 이력 요약

### 백엔드 (2026-02-08)

| 파일 | 변경 유형 | 상태 |
|------|----------|------|
| `requirements.txt` | playwright 추가 | 완료 |
| `src/core/config.py` | 브라우저/크롤링 설정 5개 추가 | 완료 |
| `src/connectors/base.py` | BrowserError 계열 3개 예외 추가 | 완료 |
| `src/connectors/kb_endpoints.py` | 신규 → 실제 API 경로로 업데이트 | 완료 |
| `src/connectors/kb_base.py` | 신규 (하이브리드 베이스 커넥터) | 완료 |
| `src/connectors/kb_price.py` | Mock → 실제 구현 + 파서 업데이트 | 완료 |
| `src/connectors/kb_transaction.py` | 신규 (실거래가 커넥터) | 완료 |
| `src/connectors/kb_listing.py` | Mock → 실제 하이브리드 구현 | 완료 |
| `src/connectors/__init__.py` | 새 커넥터/예외/엔드포인트 export | 완료 |
| `src/models/crawl.py` | KB_TRANSACTION JobType 추가 | 완료 |
| `src/models/price_data.py` | Boolean 임포트 수정 | 완료 |
| `src/workers/tasks.py` | 6개 신규 태스크 추가 | 완료 |
| `src/workers/celery_app.py` | Beat 스케줄 + 브라우저 정리 | 완료 |
| `src/admin_api/routers/jobs.py` | 모든 JobType 처리 + run-region | 완료 |
| `src/admin_api/routers/complexes.py` | discover-region 엔드포인트 | 완료 |
| `src/browser/*` | 신규 패키지 (3개 파일) | 완료 |
| `src/services/complex_discovery.py` | 신규 (지역 단지 발견) | 완료 |
| `scripts/run_api_discovery.py` | 신규 (API 발견 CLI) | 완료 |

### 프론트엔드 (2026-02-09)

| 파일/디렉토리 | 설명 | 상태 |
|---------------|------|------|
| `frontend/` | 프로젝트 전체 (Vite + React + TS) | 완료 |
| `frontend/src/api/` | API 클라이언트 (5개 파일) | 완료 |
| `frontend/src/hooks/` | TanStack Query 훅 (4개 파일) | 완료 |
| `frontend/src/types/` | TypeScript 타입 (4개 파일) | 완료 |
| `frontend/src/lib/` | 유틸리티 (3개 파일) | 완료 |
| `frontend/src/components/` | UI/레이아웃/공통/차트 (22개 파일) | 완료 |
| `frontend/src/pages/` | 페이지 컴포넌트 (11개 파일) | 완료 |

### 인프라 (2026-02-09)

| 파일 | 변경 유형 | 상태 |
|------|----------|------|
| `alembic.ini` | `script_location` 버그 수정 | 완료 |
| `.env` | 신규 생성 (포트 5433) | 완료 |
| `docker-compose.yml` | PG 포트 5433 매핑 | 완료 |
| `src/alembic/versions/` | 초기 마이그레이션 파일 | 완료 |
